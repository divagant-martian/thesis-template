\addchap{Sección de referencia}

\section*{Citaciones a bibliografía}
\tinylipsum Tomado de \cite{latex}.

\section*{Notas al pie}
\tinylipsum \footnote{Properly formatted clickable URL: \url{https://www.tum.de/}}

\section*{Listas}
\tinylipsum

\tinylipsum
\begin{enumerate}
	\item \tinylipsum
	\item \tinylipsum
	\item \tinylipsum
	\item \tinylipsum!
\end{enumerate}

\begin{itemize}
	\item list
	\item all
	\item the
	\item things!
\end{itemize}
\tinylipsum

\section*{Comntarios al margen}
Texto acá \comment{Comentarios allá}

\section*{Figuras}
% Filename, Caption, Label, Width % of textwidth
\tinylipsum
\insertfigure{images/swing_function_plot.png}%
{Plot de una función}{fig:fnplot}{0.3}
\tinylipsum

\section*{Tablas}

\tinylipsum

\inserttable{Stuffed animals}{@{}llr@{}}{
  \multicolumn{2}{c}{Item} \\
  \cmidrule(r){1-2}
  Animal & Description & Price (\$)\\
  \midrule
  Gnat  & per gram  & 13.65 \\
  & each      & 0.01 \\
  Gnu   & stuffed   & 92.50 \\
  Emu   & stuffed   & 33.33 \\
  Armadillo & frozen & 8.99 \\
}{stuffed-animals}

\tinylipsum

\section*{Including code}

Code can be made using the package
\href{https://www.sharelatex.com/learn/Code\_Highlighting\_with\_minted}{Minted}.

An exaple of which of can be found below (see Source Code \ref{lst:nice_listing})

\begin{listing}[H]
	%the language syntax can be declared here.
	\begin{minted}{python}
	import numpy as np

	def incmatrix(genl1,genl2):
	    m = len(genl1)
	    n = len(genl2)
	    M = None #to become the incidence matrix
	    VT = np.zeros((n*m,1), int)  #dummy variable

	    #compute the bitwise xor matrix
	    M1 = bitxormatrix(genl1)
	    M2 = np.triu(bitxormatrix(genl2),1)

	    for i in range(m-1):
	        for j in range(i+1, m):
	            [r,c] = np.where(M2 == M1[i,j])
	            for k in range(len(r)):
	                VT[(i)*n + r[k]] = 1;
	                VT[(i)*n + c[k]] = 1;
	                VT[(j)*n + r[k]] = 1;
	                VT[(j)*n + c[k]] = 1;

	                if M is None:
	                    M = np.copy(VT)
	                else:
	                    M = np.concatenate((M, VT), 1)

	                VT = np.zeros((n*m,1), int)

	    return M
	\end{minted}
  \addcaption{My nice listing}
  \label{lst:nice_listing}
\end{listing}

\section*{Anotaciones sobre el pdf}

\tinylipsum \feedback{Agregar un lipsum de ejemplo}\feedbackans{Se agregó un lipsum no tan grande, pero se agregó} \tinylipsum \feedback{Comentarios que nos hayan hecho} \tinylipsum

\section*{Referencias a partes del texto}
Acá referenciamos la \rsec{section:lecciones-aprendidas} dentro del \rchap{chapter:conclusions} y el código en \ref{lst:nice_listing}
